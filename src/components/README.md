# components

Shared presentational building blocks.

- `ui/` — primitive design-system controls (Button, Input, …)
- `shared/` — composite helpers used across features (placeholders, theme toggle)
- `layout/` — route shells (auth, customer, restaurant)
