# routes

React Router v7 configuration.

- `paths.ts` — path constants (`/login`, `/customer/home`, `/restaurant/dashboard`, …)
- `role-config.ts` — role → home path map
- `app-routes.tsx` — public, guest, protected, and role-guarded trees
- `AppRouter.tsx` — `createBrowserRouter` provider
