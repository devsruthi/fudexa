# routes

React Router v7 configuration.

- `paths.ts` — path constants
- `role-config.ts` — role → home path map (extend for new roles here)
- `app-routes.tsx` — public, auth, protected, and role-guarded route trees
